// Initialize VANTA NET effect
VANTA.NET({
  el: "#vanta-background",  // target the background div
  mouseControls: true,
  touchControls: true,
  gyroControls: false,
  minHeight: window.innerHeight,  // full height
  minWidth: window.innerWidth,    // full width
  scale: 1.00,
  scaleMobile: 1.00,
  color: 0xb400ff,
  backgroundColor: 0x0
})